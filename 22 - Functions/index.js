//function = a section of reusable code. atau sama aja kea def di python
// declare code once, use it whenever you wanted,call the function to execute code

function bday(username,age){
    console.log(`${username} Happy Birthday!, your now ${age} years old!`);
}

//call the func
bday("Xnaizer",23);

// dari function ini kita bisa menggukan banyak fungsi code yang dapat digunakan berkali kali dan dapat dipanggil terus, lebih kurangnya mirip seperti python hanya saja python menggukan def